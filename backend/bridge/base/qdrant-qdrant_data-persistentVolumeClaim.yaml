#! qdrant-qdrant_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: qdrant-qdrant-data
    namespace: backend
    labels:
        com.docker.compose.service: qdrant
        com.docker.compose.volume: qdrant_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
